# randomize names
names <- c("Luciano", "Mark", "Dan", "Sam", "Sarah", "Bryan")
randomized_names <- sample(names)
# add commas
cat(paste(randomized_names, collapse = ", "))

# print(randomized_names)
